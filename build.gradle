plugins {
    id 'idea'
}

subprojects {
    apply plugin: 'idea'
    apply plugin: 'java'
}

wrapper {
    gradleVersion = '5.6.4'
    distributionUrl = "https://services.gradle.org/distributions/gradle-${gradleVersion}-all.zip"
}
