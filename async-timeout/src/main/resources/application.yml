spring:
  application:
    name: async-timeout
server:
  port: 9000
graphql:
  servlet:
    exception-handlers-enabled: true
    async:
      timeout: 500
      enabled: true
  graphiql:
    enabled: true
